# AGENTS.md

## Project Overview
- Python project managed with local tooling.

## Repository Structure
- Keep application code and tests clearly separated.
- Follow existing package layout before introducing new modules.

## Coding Standards
- Prefer type hints on public APIs.
- Use `ruff` and `pytest`.

## Test Commands
- `pytest`

## Commit Checklist
- Run relevant tests.
- Run linting if configured.
- Keep commits scoped to one concern.

## Local Model Constraints
- Keep prompts concise.
- Prefer targeted context over large file dumps.

