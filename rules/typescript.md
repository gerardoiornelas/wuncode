---
globs: ["**/*.ts", "**/*.tsx", "**/package.json", "**/tsconfig.json"]
---
# TypeScript Rules

- Prefer explicit types at module boundaries.
- Keep React components small and composable.
- Run the repo's lint and typecheck commands before finishing.

