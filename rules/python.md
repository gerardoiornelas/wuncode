---
globs: ["**/*.py", "**/requirements.txt", "**/pyproject.toml"]
---
# Python Rules

- Prefer type hints on public function signatures.
- Use `ruff` for linting and `pytest` for tests.
- Keep lines under 100 characters unless the project says otherwise.

