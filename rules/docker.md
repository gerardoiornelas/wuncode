---
globs: ["**/Dockerfile", "**/docker-compose.yml", "**/docker-compose.yaml"]
---
# Docker Rules

- Keep images minimal and deterministic.
- Pin base images intentionally.
- Document required runtime ports and env vars.

