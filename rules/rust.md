---
globs: ["**/*.rs", "**/Cargo.toml"]
---
# Rust Rules

- Prefer clear ownership and borrowing over unnecessary cloning.
- Run `cargo fmt` and targeted tests where possible.
- Keep error handling explicit.

